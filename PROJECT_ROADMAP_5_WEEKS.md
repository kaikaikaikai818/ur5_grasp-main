# 五类典型工具自主抓取与固定位置递送：五周推进路线

## 当前基线

- 已完成红色圆柱的手外粗定位、手内精定位、预抓取、下降、夹紧和抬升。
- 当前真机基线脚本为 `dual_camera_cylinder_grasp.py`。
- 活动工具中心点为示教器中的 `TCP_clamp`，Python不重复添加265 mm工具长度。
- 手外相机Z补偿默认值为26 mm，圆柱高度默认值为50 mm。
- 在进入五类工具开发前，先完成一键圆柱抓取的重复性验证并冻结基线。

## 第一周：验证并冻结圆柱基线

### 明天的真机任务

1. 在真机电脑执行 `git pull origin main`，确认包含提交 `0ee6da8` 或更新版本。
2. 保持示教器活动TCP为 `TCP_clamp`，夹爪下方和机器人路径内无人、无障碍物。
3. 先完成3次低风险一键抓取，再累计完成10次。
4. 每次将终端输出和结果填写到 `templates/cylinder_baseline_trials.csv`。
5. 若失败，只记录并分类原因；同一天不要同时修改外参、运动高度和抓取偏移。

### 冻结条件

- 10次中至少8次成功夹起。
- 没有越界下降或碰撞。
- 检测失败、坐标不稳定或深度异常时，程序能在下降前停止。
- 手内锁定坐标和抓取落点误差呈稳定偏差，而不是无规律跳变。

达到条件后冻结相机序列号、标定文件、TCP、工作空间、运动速度、观察高度、预抓取高度和双相机状态机。后续五类工具开发通过新模块扩展，不直接重写该基线。

### 工具定义

本周确定不少于五种工具的固定类别名，并填写 `templates/tool_grasp_profiles.csv`。每类至少明确：

- 实物名称和尺寸范围；
- 允许夹持区域与禁止夹持区域；
- 期望夹爪方向；
- 夹持中心偏移；
- 预抓取高度和夹持高度；
- 夹爪闭合位置、速度和力度。

## 第二周：YOLO-seg数据集

- 每类采集150–250张有效图片，总量约1000–1500张。
- 覆盖不同位置、0–360°方向、光照、阴影、单工具、多工具和少量轻微遮挡。
- 使用多边形掩码标注完整轮廓，不只标矩形框。
- 类别名称与抓取配置表完全一致，确定后不再改名。
- 按拍摄批次划分训练、验证和测试集，避免连续帧泄漏到不同集合。
- 每类至少准备2–3个实物；只有一个实物时，通过位置、方向、距离和光照增加变化。

详细要求见 `DATASET_COLLECTION_GUIDE.md`。

## 第三周：训练和双相机接入

- 训练轻量级YOLO-seg基线模型，SAM3仅用于试验或辅助标注。
- 输出统一为：类别、置信度、掩码、像素中心、主轴角、深度和基座坐标。
- 先完成纯相机模式，确认两台相机能稳定识别指定类别。
- 再将检测接口接入已经验证的圆柱状态机，只替换检测与抓取点计算部分。
- 控制场景目标：五类召回率不低于90%，连续8帧中心与方向稳定，不在同场工具间跳变。

## 第四周：五类抓取和固定位置递送

1. 每轮只摆一种工具，逐类验证抓取策略。
2. 多工具同场，由操作者选择目标类别，机器人只抓指定工具。
3. 为每类分别调整夹持区域、中心偏移、方向和夹爪参数。
4. 在示教器中测量固定递送点及其上方安全点，记录为基座坐标。
5. 递送执行“安全点—垂直下降—松开—垂直抬升”，不依赖相机重新定位递送点。

## 第五周：系统实验

- 每类至少10次，总计不少于50次。
- 每类抓取成功率目标不低于80%，总体固定位置递送成功率不低于80%。
- 记录识别、坐标、抓取方向、夹持结果、移动滑落、递送结果和失败原因。
- 漏检/误检通过补数据处理；稳定偏移通过抓取配置处理；滑落通过夹持区域和夹爪参数处理；递送误差只调整递送位。

## 每周节奏

- 真机日1：集中测试并保留终端日志、照片和失败编号。
- 离线日：只分析一个主要问题并修改对应模块。
- 真机日2：验证修改是否解决该问题。
- 真机日3–4：做重复性实验和补拍数据。
- 每周末形成一页总结：完成项、成功率、主要失败、下周唯一优先目标。

## 后续递送到人手

固定位置递送稳定后再依次实现：固定人机交接点、检测人已取走、根据手部位置调整交接点。该阶段必须另加速度限制、安全距离、交接确认和异常撤回，不纳入当前五周验收。
